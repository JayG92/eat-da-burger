<div>
<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Pick a Burger to DEVOUR!</h1>
    <p class="lead"></p>
  </div>
</div>

<h2>Menu</h2>

<ul>
  {{#each burgers}}
    {{#unless sleepy}}
      {{> burgers/burger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h5>DEVOURED!</h5>

<ul>
  {{#each burgers}}
    {{#if sleepy}}
      {{> burgers/burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Hungry?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Devoured!<br>
    <input type="radio" name="sleepy" value="0"> Not Hungry!
  </div>

  <button class="submitBtn" type="submit">Add Burger</button>
</form>
</div>